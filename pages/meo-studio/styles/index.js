import React from 'react'

function Index() {
    const string = "No, there's nothing here..."
    const string_2 = "There's a way to prevent this, by checking the index file in every folder. But i'm kinda lazy..."
  return (
    <div>
        <div>{string}</div>
        <div>{string_2}</div>
    </div>
    
  )
}

export default Index